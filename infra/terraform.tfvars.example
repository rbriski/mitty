# Hetzner Cloud API token (from https://console.hetzner.cloud/)
hcloud_token = "your-hcloud-api-token"

# DNSimple (from https://dnsimple.com/user)
dnsimple_token      = "your-dnsimple-api-token"
dnsimple_account_id = "12345"

# Server configuration
server_name     = "mitty"
server_type     = "cx23"
server_location = "fsn1"
server_image    = "ubuntu-24.04"

# SSH key path
ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# Domain configuration
domain    = "mitty.ayla.works"
zone_name = "ayla.works"

# Restrict SSH access to your IP (recommended)
# ssh_allowed_ips = ["YOUR.IP.ADDRESS/32"]
